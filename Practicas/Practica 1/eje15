      /*
        El problema en el codigo es que hay dos variables declaradas con el mismo nombre
        si se elimina la declaracion de una variable, el programa anda correctamente. 
        
      */  
        
        ==> programa con error
        int i = 0;
        for (int i = 1; i <= 10;)
         {
            Console.WriteLine(i++);
         }

         ==> error solucionado
        for (int i = 1; i <= 10;)
         {
            Console.WriteLine(i++);
         }