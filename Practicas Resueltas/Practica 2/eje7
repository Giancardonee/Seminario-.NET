        char c1 = 'A';
        char c2 = 'A';
        Console.WriteLine(c1 == c2); 
      /*
        Imprime true .porque char es de tipo valor, por lo tanto compara el valor de cada 
        variable, y c1 es igual c2. 
      */   
        object o1 = c1;
        object o2 = c2;
        Console.WriteLine(o1 == o2);
    /*
        Imprime false .porque object es de tipo referencia, entonces en este caso, se esta
        preguntando si las dos variables tienen la misma referencia, o dicho de otra forma, 
        si apuntarian al mismo lugar en memoria heap. 
        Con variables de tipo referencia no se compara el contenido, sino la referencia
    */ 