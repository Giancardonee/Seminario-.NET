int i = 1;

 if (--i == 0) ==> Primero decrementa y despues evalua
 {
 Console.WriteLine("cero"); ==> entra al if y imprime cero
 }
 if (i++ == 0) ==> Primero evalua y incrementa despues, no entra al if
 {
 Console.WriteLine("cero");
 }
 Console.WriteLine(i); ==> Imprime cero (a esta altura del codigo ya se incrementó i)

 ==>>> cuando hay un -- o ++ ANTES de una variable : --i o ++i se lo conoce como prefijo
 PRIMERO INCREMENTA O DECREMENTA y despues si es necesario evalua
 ==>> cuando hay un -- o ++ DESPUES de una variable : i++ o i-- se lo conoce como sufijo
 PRIMERO EVALUA (si hay una evaluacion) y despues Incrementa o Decrementa