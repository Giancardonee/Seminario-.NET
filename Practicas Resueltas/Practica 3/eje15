        int x = 0;
        try
        {
        Console.WriteLine(1.0 / x);
        Console.WriteLine(1 / x);
        Console.WriteLine("todo OK");
        }
        catch (Exception e)
        {
        Console.WriteLine(e.Message);
        }
        La división por 0 en C# con números enteros (int, long, etc.) 
        genera una excepción (System.DivideByZeroException) en lugar de devolver Infinity o NegativeInfinity,
        comm hace la division de un double por 0, 
        ya que la aritmética de enteros no admite divisiones por 0.}+

        